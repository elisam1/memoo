# Memoo Environment Configuration

# Secret used to sign sessions
SESSION_SECRET=change_me_in_production

# Allow any email domain to login (true/false)
# If false, only domains listed in data/companies.json can login
ALLOW_ANY_DOMAIN=false

# Example: SMTP settings come from data/companies.json per domain
# See data/companies.json to add your company domain and SMTP config
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_mailtrap_username
SMTP_PASS=your_mailtrap_password
MAIL_FROM=Memoo <no-reply@yourdomain.com>
FALLBACK_HR_EMAIL=

# Firebase Admin configuration (choose ONE of the following)
# Option A: Path to service account JSON file (recommended for local dev)
# Example: put serviceAccount.json in project root and set:
# FIREBASE_SERVICE_ACCOUNT_FILE=./serviceAccount.json
FIREBASE_SERVICE_ACCOUNT_FILE=

# Option B: Raw JSON string (be careful with escaping in .env)
FIREBASE_SERVICE_ACCOUNT_JSON=

# Option C: Base64-encoded JSON string
FIREBASE_SERVICE_ACCOUNT_BASE64=

# Optionally override project id (otherwise uses service account's project_id)
FIREBASE_PROJECT_ID=
