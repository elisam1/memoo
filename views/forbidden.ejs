<section class="card-grid">
  <div class="card">
    <h2>Access Restricted</h2>
    <p class="muted">
      <% if (roleRequired) { %>
        This area is for <strong><%= roleRequired.toUpperCase() %></strong> only.
      <% } else { %>
        You don’t have permission to view this page.
      <% } %>
    </p>

    <div class="actions" style="margin-top:12px;">
      <a class="btn" href="/home">Go Home</a>
      <% if (user && user.role === 'manager') { %>
        <a class="btn" href="/manager">Manager Dashboard</a>
      <% } %>
      <% if (user && user.role === 'hr') { %>
        <a class="btn" href="/hr">HR Dashboard</a>
      <% } %>
      <% if (user && user.role === 'admin') { %>
        <a class="btn" href="/admin/domains">Manage Domains</a>
      <% } %>
    </div>
  </div>
</section>
